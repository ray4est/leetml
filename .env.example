# Use a unique passphrase with at least 20 characters for the two trusted users.
APP_ACCESS_PASSWORD=

# Generate at least 32 random bytes, for example: openssl rand -base64 32
SESSION_SECRET=

# Create these credentials in your Modal workspace settings.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
